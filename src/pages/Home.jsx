import { HomeTitle } from './Home.styled';

const Home = () => {
  return (
    <section
      style={{
        padding: '30px',
      }}
    >
      <HomeTitle> 
        Phonebook welcome page
      </HomeTitle>
    </section>
  );
};

export default Home;
